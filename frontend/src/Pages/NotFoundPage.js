import React from 'react';
import { Box, Text } from '@chakra-ui/react';
import Layout from '../components/layout';

const NotFoundPage = () => {
  return (
    <Layout>
      <Box>
        <Text>I cannot be found.</Text>
      </Box>
    </Layout>
  );
};

export default NotFoundPage;
